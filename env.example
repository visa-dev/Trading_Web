POSTGRES_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

PRISMA_DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_PRISMA_ACCELERATE_API_KEY"

# Production connection (requires PostgreSQL)
DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Local development example (uncomment to use SQLite)
# DATABASE_URL="file:./dev.db"

# NextAuth configuration
NEXTAUTH_SECRET="REPLACE_WITH_STRONG_SECRET"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (if using Google sign-in)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Vercel Blob storage token
BLOB_READ_WRITE_TOKEN="your-vercel-blob-token"

# Optional: SMTP credentials for email notifications
# EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"
# EMAIL_FROM="Trading Platform <no-reply@example.com>"

